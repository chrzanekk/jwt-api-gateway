server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

spring:
  application:
    name: discovery-service

management:
  zipkin:
    tracing:
      endpoint: http://zipkin:9941